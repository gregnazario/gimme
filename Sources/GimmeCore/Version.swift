import Foundation

/// The gimmie version string. Updated by the release CI from the git tag.
/// Defaults to "dev" during local builds.
public enum GimmeVersion {
    public static let current = "2.0.0-dev"
}
